'use strict'
require('../../setup')

import {tmp} from 'tmp'
import FileSystem from '../../../src/utils/FileSystem'

contract('FileSystem', () => {
  it('can remove a directory', async function () {
    var testDir = tmp.dirSync();
    FileSystem.exists(testDir).shoud.be.true;
  })

})
